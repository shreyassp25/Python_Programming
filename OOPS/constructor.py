# there is a constructor in every class...this constructor is also called as the init function.
# this _init_() function gets automatically invoked/executed every time a new object is created .

"""
while creating a class if a constructor/init function is initialised, whenever an object is created
of that class, the init function gets called.
"""


class Student:
    def __init__():
        print("adding new student to db...." )

s1=Student()

# when this is run...u get output - adding new student to the db
# as when a new object is created, the constructor is called...the init function is called.


